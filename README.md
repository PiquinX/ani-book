To DO:


- See if there is a method on the API to delete an anime/book from the list, if there is add the functionality with a confirmation modal and a succesfully deleted message
- Improve the log in with Google UI
- Add a share button to share an entertainment item

- Add a start menu to select between anime and books



- Implement Pagination or Virtualization: Only render the items that are visible on the screen.
- Export list to a txt file with title and rate
- Export list to a JSON file with all the data

To Consider:
- Leave only animes and books

- Try adding a manga list(I'll need to update the API)
- Create a settings option to be able to hide/show some things like the rate, description, the books list, the movies list, the series list, the manga list(if I add it), etc.

- Try adding the same title autocompletion to movies, series and books and manga (if I add it), look for a free API for each of them.